server:
  listen_host: "0.0.0.0"
  listen_port: 9999
  client_reply_port: 8888

model:
  path: "models/raven_v2.pt"
  imgsz: 640
  conf_threshold: 0.4
  iou_threshold: 0.45
  half: true

gpus: [0, 1, 2]

queues:
  max_depth: 10

api:
  host: "0.0.0.0"
  port: 8080

logging:
  level: "INFO"
  structured: true
